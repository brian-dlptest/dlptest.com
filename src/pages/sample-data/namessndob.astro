---
import BaseLayout from "@/layouts/BaseLayout.astro";
export const prerender = true;
// TODO: Port the names/SSN/DOB table from the legacy site (https://dlptest.com/sample-data/namessndob/).
---
<BaseLayout title="Sample Data — Names + SSN + DOB">
  <h1>Names + SSN + Date of Birth</h1>
  <p class="muted">
    This variant of the sample table is still being ported from the legacy
    WordPress site. For now, see the
    <a href="/sample-data/">primary sample data page</a>.
  </p>
</BaseLayout>
