{
  "name": "dlptest.com",
  "private": true,
  "version": "0.0.1",
  "description": "DLP Test site — Cloudflare Workers (staging via wrangler env)",
  "scripts": {
    "dev": "wrangler dev",
    "dev:staging": "wrangler dev --env staging",
    "check": "wrangler check",
    "deploy": "wrangler deploy --env=\"\"",
    "deploy:staging": "wrangler deploy --env staging"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250425.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.19.0"
  }
}
