# Local FTP / file backups (may contain wp-config.php, uploads, secrets)
backup/

.DS_Store
node_modules/
.wrangler/
.dev.vars
.env
.env.*
!.env.example

# Astro build output and cache
dist/
.astro/

# Local scratch caches for one-shot import scripts (regenerable from the source URLs)
scripts/news/cache/
